{
  "name": "@comfy/agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The rippel agent: installs and manages ComfyUI on one machine.",
  "bin": { "rippel-agent": "src/main.mjs" },
  "scripts": {
    "start": "node src/main.mjs",
    "build:release": "node scripts/build-release.mjs",
    "test": "node --test test/*.test.mjs"
  },
  "engines": { "node": ">=20" }
}
